# Copy this to .env.local and fill in your actual API keys
# This file will be gitignored for security

# Supabase (You already have these)
VITE_SUPABASE_URL=your_existing_supabase_url
VITE_SUPABASE_ANON_KEY=your_existing_supabase_key

# Security APIs (Fill these in after signup)
SONAR_TOKEN=                    # Get from https://sonarcloud.io > My Account > Security
SONAR_URL=https://sonarcloud.io
SONAR_PROJECT_KEY=TechTyphoon_secure-flow-automaton  # Your GitHub repo format

SNYK_TOKEN=                     # Get from https://snyk.io > Settings > API Token  
SNYK_ORG_ID=                    # Your Snyk organization ID

GITHUB_TOKEN=                   # Personal Access Token from GitHub Settings

# Notification Webhooks (Configure after app creation)
SLACK_WEBHOOK_URL=              # From Slack App > Incoming Webhooks
SLACK_CHANNEL=#security-alerts  # Your preferred channel

# Optional: Teams Integration
TEAMS_WEBHOOK_URL=              # From Teams > Connectors > Incoming Webhook

# Container Security (Optional - for Docker scanning)
DOCKER_HUB_USERNAME=            # Your Docker Hub username
DOCKER_HUB_TOKEN=               # Docker Hub access token
